steps:
  lint:
    image: rust:alpine
    commands:
      - rustup component add clippy
      - cargo clippy --verbose

  test:
      image: rust:alpine
      commands:
        - apk add libpq-dev 
        - cargo test

  build:
    image: rust:alpine
    commands:
      - apk add libpq-dev
      - cargo build --release

when:
  - event: [pull_request, manual]